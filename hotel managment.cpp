#include<iostream>
#include"hotel structures.h"// user defined library that holds all the structures used in the program 
#include"hotel functions.h"// user defined library that holds all the functions used in the program
using namespace std;

int main()
{
	roomDescription(room);//creating hotel rooms
	InitializeWebpage(); // calling a function that displays homepage of the program
	return 0;
}
